name: pipeline_bug_income_50pct
description: 50% null injection on income should be diagnosed as a pipeline bug requiring human escalation
setup:
  traffic:
    month: 0
    rows: 1000
    speed: 1000.0
  injection:
    type: nulls
    feature: income
    rate: 0.5
expected:
  diagnosis: pipeline_bug
  action: alert_human
